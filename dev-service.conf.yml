# example default dev-service.conf.yml for test
# This element is setting for running test/stub/proc0.js
- 
  nameTag: Default # needed, as the name tag show in console
  color: yellow # optional, the color of name tag
  command: node test/stubs/proc1.js # needed, command to start this service 
